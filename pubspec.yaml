name: stream_chat_demo
description: 'A new Flutter project.'
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.2.3

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  stream_chat_flutter: 8.0.0-beta.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
